#version 450 core

layout (local_size_x=1) in;

layout(std140,set=0,binding=0) buffer  inDat{
    vec3 data[];
} inInt;

//layout(set=0,binding=1) buffer outDat{
//    float dat[];
//}outInt;

void main(){
    uint glID = gl_GlobalInvocationID.x;
        if(glID <= 1){


        for(int i=1;i<100;i++){

           inInt.data[i]= vec3(1,2,3);
        }


    }

}